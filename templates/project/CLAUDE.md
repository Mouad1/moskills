# PROJECT_NAME

> One-line purpose of this project. REPLACE THIS.

<!-- moskills:begin (managed imports - do not edit inside markers) -->
@.claude/standard/base.md
@.claude/standard/session-protocol.md
<!-- moskills:end -->

## Stack

- REPLACE: framework, language, database, deploy target.

## Project constraints

<!-- The 3-5 NON-OBVIOUS rules an agent must never break in THIS repo. Examples:
- Never import pipeline code into API routes (module isolation).
- No new dependencies without asking.
- No schema change without a migration plan. -->
- REPLACE ME

## Commands

- dev: `REPLACE`
- test: `REPLACE`
- build: `REPLACE`

## Read next

- @.claude/STATE.md — current state, blockers, next steps
- @tasks/lessons.md — mistakes already made; do not repeat them
- @docs/ARCHITECTURE.md — system overview (create it if missing)
