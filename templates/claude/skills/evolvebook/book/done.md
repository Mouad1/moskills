# Done list — {{name}}

What we already made, one row per run. The repeat check compares a new plan
with these rows on the Choices columns. Never edit a row to pass the check.

| Date | What | Manual steps |
|---|---|---|
