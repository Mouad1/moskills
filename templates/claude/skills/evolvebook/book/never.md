# Never list — {{name}}

Things we refuse. Every rule says what to do instead. Rules are added only
after the user says yes.

Entry format:

    - Never: what we refuse
      Instead: what we do
      Since: YYYY-MM-DD (why, in one line)
