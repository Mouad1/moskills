# Choices — {{name}}

The decisions made every run. Each one is a column of the Done list, so the
repeat check can tell two runs apart.

Repeat threshold: the plan must differ from every earlier run on at least half
of the choices (rounded up). To change it, add a line `Repeat threshold: N`.
