# Toolbox — {{name}}

Ready-made templates, checklists and scripts. Starts empty: an item is added
only when the same manual step shows up in 3 runs and the user says yes.

One line per item:

    - manual step it replaces -> file name
