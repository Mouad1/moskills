# /preview

Manual trigger for the `preview` skill.

Run it to turn a vague idea into a written spec before any code is written. The full
procedure, output template, and rules live in the skill, which also runs automatically
when its `Use when:` triggers match.
