# /memorize

Manual trigger for the `memorize` skill.

Run it to check, write, or summarize durable project memory without filling the
chat. The retrieval flow and rules live in the skill, which also runs
automatically when its `Use when:` triggers match.
