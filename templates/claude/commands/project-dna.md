# /project-dna

Manual trigger for the `project-dna` skill.

Run it to log what was just built, configured, or decided into `PROJECT-DNA.md`
as a structured, reproducible entry with a Replay Prompt.

The skill also fires automatically before `git commit` or `git push` to capture
work before it is sealed into version history.
