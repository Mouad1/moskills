# /compress-input

Manual trigger for the `compress-input` skill.

Run it to switch the agent to short, direct, low-noise communication. The modes
(lite, full, ultra, wenyan) and rules live in the skill, which also runs
automatically when its `Use when:` triggers match.
